import React from "react"; // Make sure to import React if you're not using a JSX transformer like Babel.

import Presentation from "../../Pages/Presentation";

export default function PresentationPage() {
  return <Presentation />;
}
